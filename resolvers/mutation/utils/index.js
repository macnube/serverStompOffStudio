const {
    sendEmail,
    cardFinishedText,
    cardExpiredText,
    cardOverdueText,
} = require('./email');

module.exports = {
    sendEmail,
    cardFinishedText,
    cardExpiredText,
    cardOverdueText,
};
