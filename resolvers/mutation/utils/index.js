const {
    sendEmail,
    cardFinishedText,
    cardExpiredText,
    cardOverdueText,
    resetPasswordText,
} = require('./email');

module.exports = {
    sendEmail,
    cardFinishedText,
    cardExpiredText,
    cardOverdueText,
    resetPasswordText,
};
